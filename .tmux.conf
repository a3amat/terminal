# меняем префикс с ctrl+b на ctrl+a
set -g prefix C-a
bind C-a send-prefix

# включаем поддержку мыши
set -g mouse on

# Разделение терминалов с помощью | и -
unbind '"'
unbind %
bind - split-window -h
bind | split-window -v
